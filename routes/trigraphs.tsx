import { define } from "@/utils/define.ts";
import TrigraphsTyperMode from "../islands/TrigraphsTyperMode.tsx";

export default define.page(function TrigraphsPage() {
  return <TrigraphsTyperMode />;
});
